#ifndef DEF_EVENTS_HPP
#define DEF_EVENTS_HPP

#include <SDL.h>

void UpdateKeys(const SDL_Event &event);
void UpdateMouse(const SDL_Event &event);
//void UpdateWindow(const SDL_Event event);

#endif //DEF_EVENTS_HPP